# Alumini - Backend (no DB)
Simple Express backend that stores data in memory (no database).
Default admin credentials:
- email: admin@college.com
- password: admin123

Install & run:
```
cd backend
npm install
npm run start
```
For development with auto-reload:
```
npm install
npm run dev
```
